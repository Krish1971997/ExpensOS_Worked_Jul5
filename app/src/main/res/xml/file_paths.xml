<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Matches new File(getCacheDir(), "receipts") in
         TransactionEntryActivity.launchCamera() -->
    <cache-path
        name="receipts"
        path="receipts/" />
    <external-path
        name="external_files"
        path="." />
    <cache-path
        name="cache"
        path="." />
    <!-- Matches new File(getCacheDir(), "reports") in
         GenerateReportActivity.previewPdf() -->
    <cache-path
        name="reports"
        path="reports/" />
</paths>
